# Firebase config — contains project keys; provide locally / via CI secret
google-services.json
android/app/google-services.json

# Android build output (the android/ project itself IS committed)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/

# Signing secrets + build artifacts (never commit these)
*.jks
*.keystore
keystore.properties
*.apk
*.aab
